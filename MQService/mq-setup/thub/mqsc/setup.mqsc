DEFINE QLOCAL('NA.CITIFT.THUB.LISTEN') REPLACE
DEFINE QLOCAL('THUB.TO.CFTNA.TMQ') USAGE(XMITQ) REPLACE
DEFINE QREMOTE('NA.THUB.CITIFT.ACK.REMOTE') RNAME('NA.THUB.CITIFT.ACK.LISTEN') RQMNAME('CITIFT') XMITQ('THUB.TO.CFTNA.TMQ') REPLACE
DEFINE CHANNEL('THUB.TO.CFTNA') CHLTYPE(SDR) TRPTYPE(TCP) CONNAME('172.19.0.3(1414)') XMITQ('THUB.TO.CFTNA.TMQ') REPLACE
DEFINE CHANNEL('CFTNA.TO.THUB') CHLTYPE(RCVR) TRPTYPE(TCP) REPLACE
START CHANNEL('THUB.TO.CFTNA')

//docker inspect -f '{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}' mq-thub
