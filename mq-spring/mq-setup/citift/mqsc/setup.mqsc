DEFINE QLOCAL('NA.THUB.CITIFT.ACK.LISTEN') REPLACE
DEFINE QLOCAL('CFTNA.TO.THUB.TMQ') USAGE(XMITQ) REPLACE
DEFINE QREMOTE('NA.CITIFT.THUB.REMOTE') RNAME('NA.CITIFT.THUB.LISTEN') RQMNAME('THUB') XMITQ('CFTNA.TO.THUB.TMQ') REPLACE
DEFINE CHANNEL('CFTNA.TO.THUB') CHLTYPE(SDR) TRPTYPE(TCP) CONNAME('thub(1414)') XMITQ('CFTNA.TO.THUB.TMQ') REPLACE
DEFINE CHANNEL('THUB.TO.CFTNA') CHLTYPE(RCVR) TRPTYPE(TCP) REPLACE
START CHANNEL('CFTNA.TO.THUB')
