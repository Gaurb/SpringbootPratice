DEFINE QLOCAL('NA.CITIFT.THUB.LISTEN') REPLACE
DEFINE QLOCAL('THUB.TO_THUB.TMQ') USAGE(XMITQ) REPLACE
DEFINE QREMOTE('NA.THUB.CITIFT.ACK.REMOTE') RNAME('NA.THUB.CITIFT.ACK.LISTEN') RQMNAME('CITIFT') XMITQ('THUB.TO_THUB.TMQ') REPLACE
DEFINE CHANNEL('THUB.TO.CFTNA') CHLTYPE(SDR) TRPTYPE(TCP) CONNAME('citift(1515)') XMITQ('THUB.TO_THUB.TMQ') REPLACE
DEFINE CHANNEL('CFTNA.TO.THUB') CHLTYPE(RCVR) TRPTYPE(TCP) REPLACE
START CHANNEL('THUB.TO.CFTNA')
